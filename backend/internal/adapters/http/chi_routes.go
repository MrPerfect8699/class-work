package http

// This file is kept for reference but is no longer used.
// Route registration is now handled directly in handler.go's RegisterRoutes() method
// which works natively with Chi router instead of requiring a wrapper.
